nodeLinker: node-modules

# The pinned toolchain versions are newer than Yarn's default minimum-release-age
# security gate (npmMinimalAgeGate, 24h), which would quarantine them and block
# `yarn install`. We pin exact versions deliberately, so disable the age gate here.
npmMinimalAgeGate: 0
